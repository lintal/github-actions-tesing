name: main-push

on:
  push:
    branches: [ main ]
    
jobs:
  do-something-test:
    runs-on: ubuntu-latest
    environment: 
      name: test
    steps:
    - run: echo 'do something on test'
  do-something-live:
    runs-on: ubuntu-latest
    environment: 
      name: live
    steps:
    - run: echo 'do something on live'
